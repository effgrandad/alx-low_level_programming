#include <stdio.h>

/**
 * main - Entry point
 * Return: always success
 */

int main(void)
{
	int 1, z = 0;

	while (1 < 1024)
	{
	if ((1 % 3 == 0) || (1 % 5 == 0))
	{
	z += i;
	}
	i++
	}
	printf("%d\n", z);
	return (0);
}
