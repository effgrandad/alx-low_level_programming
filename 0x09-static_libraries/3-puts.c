#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * Description:
 * Return: always 0
 */
int main(void)
void _puts(char *str)
{
	puts("\"programming is like building a multilinual puzzle");
	return (0);
}

